import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Start a Bounty' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
